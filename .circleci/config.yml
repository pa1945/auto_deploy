orbs:
  node: circleci/node@2.0.2

version: 2.1

jobs:
  hello-world:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout
      - run:
          name: Say Hello
          command: |
            echo "uda-deploy: Hello World"

workflows:
  matrix-tests:
    jobs:
      app-dir: frontend
      - node/test:
          version: 13.11.0
      - node/test:
          version: 12.16.0
      - node/test:
          version: 10.15.2
      - hello-world

